"""``tasks`` — a 2nd example module that **diverges** from ``notes``.

Soft-delete (``SoftDeleteMixin``) + a status filter on list. The divergence is
the point: it shows where a shared ``BaseService`` would need override hooks.
"""
