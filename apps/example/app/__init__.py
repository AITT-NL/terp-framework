"""Terp example application — a neutral dogfood app consuming packaged ``terp.core``.

It exists to exercise the kernel end-to-end (the first vertical slice) and to
surface evidence about what — if anything — should be promoted from the app into
``terp.core`` (e.g. ``create_app``, a base service). See
``docs/decisions/0001-terp-namespace-and-kernel-scope.md`` (Decision 3).
"""
