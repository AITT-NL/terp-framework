{% set singular = module_name[:-1] if module_name.endswith('s') else module_name -%}
{% set model = singular[0]|upper ~ singular[1:] -%}
"""{{ module_name }} service — CRUD inherited from BaseService (audited, OCC, scoped)."""

from __future__ import annotations

from terp.core import BaseService

from app.modules.{{ module_name }}.models import {{ model }}
from app.modules.{{ module_name }}.schemas import {{ model }}Create, {{ model }}Update


class {{ model }}Service(BaseService[{{ model }}, {{ model }}Create, {{ model }}Update]):
    model = {{ model }}
